# JARVIS PC Agent Dependencies

# Core
supabase>=2.0.0
asyncio-compat>=0.1.1

# System Control (Windows)
requests>=2.31.0           # HTTP requests for edge function calls
pycaw>=20230407          # Windows audio control
comtypes>=1.2.0          # COM interface for Windows
screen-brightness-control>=0.22.0  # Brightness control

# Input Control
pyautogui>=0.9.54        # Mouse & keyboard automation
keyboard>=0.13.5         # Low-level keyboard events
pyperclip>=1.8.2         # Clipboard access

# System Info
psutil>=5.9.0            # System and process utilities
pillow>=10.0.0           # Screenshot handling
mss>=9.0.0               # Fast multi-monitor screenshots

# Streaming / realtime
websockets>=12.0         # WS streaming (audio/camera)
pyaudio>=0.2.14          # Microphone/system audio capture
opencv-python>=4.9.0.80  # PC camera capture

# QR Code for pairing
qrcode>=7.4.2            # QR code generation for terminal display

# Voice Recognition (Wake Word)
SpeechRecognition>=3.10.0  # Voice recognition for wake word
pyttsx3>=2.90              # Text-to-speech (offline)

# System Tray (Windows)
pystray>=0.19.5          # System tray icon
win10toast-click>=0.1.2  # Windows 10/11 notifications

# Face Recognition (ML)
mediapipe>=0.10.0        # Face mesh + pose detection
numpy>=1.24.0            # Numerical operations for embeddings
scipy>=1.10.0            # Distance calculations for face matching

# Bluetooth (BLE fallback transport)
bleak>=0.21.0            # BLE client (scanning/connecting)
bless>=0.2.5             # BLE GATT server (PC acts as peripheral)

# Optional: For music features
# youtube-search-python>=1.6.6

# GUI (optional but recommended for modern dark UI)
# customtkinter>=5.2.0     # Modern dark tkinter widgets
